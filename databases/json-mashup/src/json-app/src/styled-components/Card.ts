import styled from 'styled-components';

const Card = styled.div`
  box-shadow: 1px 2px 8px rgba(0, 0, 0, .2);
  width: 80%;
  max-width: 600px;
  padding-bottom: 2px;
  border-radius: 4px;
  overflow: hidden;
  margin: 20px auto;
  text-align: center; 
`;

export default Card;
